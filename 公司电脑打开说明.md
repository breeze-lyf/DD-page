# 公司电脑打开说明

现在使用方式已经切换成 CloudBase 云端网页，不再需要在公司电脑安装 Node，也不需要复制 `new-chat` 文件夹。

## 正确方式

1. 打开 CloudBase 静态网站托管给你的正式网址。
2. 页面会先显示登录页。
3. 输入你在云函数环境变量里设置的账号和密码。
4. 进入工作台后，新增客户、随记、事项和抓取资讯都会自动保存到 CloudBase 云数据库。
5. 另一台电脑打开同一个正式网址，并输入同一个账号密码，就会读取同一份数据。

## 数据同步规则

- 新增客户：自动保存到云端。
- 新增随记：自动保存到云端。
- 新增事项记录：自动保存到云端。
- 删除记录：自动保存到云端。
- 抓取国内资讯：抓取结果直接保存到云端。
- 两台电脑同时打开时，页面会定时从云端刷新最新数据。

## 不再需要做的事

- 不需要打开 `http://127.0.0.1:8789/`。
- 不需要在公司电脑安装 Node。
- 不需要导出备份再导入。
- 不需要手动点“云同步”。

## 注意

账号密码只用于保护你的个人工作台数据，不要随便发给别人。
