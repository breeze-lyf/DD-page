# lzddd 的私人宝典：云端部署方案

目标：公司电脑不安装 Node，不复制本地网址，只用浏览器打开一个正式网址，就能记录客户、事项、随记，抓取国内资讯，并把数据保存到云数据库。

## 推荐方案

第一版建议用腾讯云 CloudBase：

- 前端网页：CloudBase 静态网站托管
- 后端接口：CloudBase HTTP 云函数 / 云托管函数
- 云数据库：CloudBase 文档型数据库
- 访问方式：公司电脑浏览器打开正式 HTTPS 网址
- 数据保护：后端设置 `APP_USERNAME` 和 `APP_PASSWORD`，网页打开前先输入账号密码，进入后不再反复弹窗

这个方案适合大陆网络，公司电脑不需要安装 Node，也不需要 VPN。

## 已经准备好的文件

前端：

```text
index.html
cloud-config.js
```

后端：

```text
cloud/cloudbase/functions/api/index.js
cloud/cloudbase/functions/api/package.json
cloud/cloudbase/functions/api/scf_bootstrap
deploy/lzddd-cloudbase-backend.zip
deploy/lzddd-cloudbase-event-backend.zip
```

## 部署步骤

### 1. 创建 CloudBase 环境

在腾讯云 CloudBase 控制台创建一个云开发环境，地域建议选上海或广州。

### 2. 创建云数据库集合

创建集合：

```text
workbench_state
```

这个集合会保存一条主文档，里面是你的客户、资讯、随记、事项记录。

### 3. 部署后端接口

把这个文件夹部署成一个 HTTP 服务：

```text
cloud/cloudbase/functions/api
```

运行环境建议：

```text
Node.js 18 或更高
```

需要设置环境变量：

```text
APP_USERNAME=lzddd
APP_PASSWORD=你自己设置的登录密码
STATE_COLLECTION=workbench_state
STATE_DOC_ID=lzddd-main
AI_BASE_URL=https://api.deepseek.com/chat/completions
AI_MODEL=deepseek-chat
AI_API_KEY=你的 DeepSeek API Key，可暂时不填
ALLOW_ORIGIN=你的前端网站域名，也可以先填 *
```

必须设置 `APP_PASSWORD`。建议同时设置 `APP_USERNAME=lzddd`，这样网页就是正常的账号密码登录。`APP_PASSWORD` 可以改成你自己好记但别人猜不到的密码。

如果 CloudBase 测试接口时报：

```text
FUNCTION_PARAM_INVALID
FunctionType parameter is invalid
```

说明当前 `api` 是 HTTP 函数，但 HTTP 访问服务的云函数路由没有接上这个函数类型。这个时候不要继续改 `/api` 路径，直接新建一个普通云函数：

```text
函数名：api2
函数类型：普通云函数 / 事件函数 / 云函数
运行环境：Node.js 18
执行方法：index.main
上传包：deploy/lzddd-cloudbase-event-backend.zip
```

然后在 HTTP 访问服务里，把 `/api` 路由关联到新的 `api2`，打开路径透传，关闭身份认证。

### 4. 配置 HTTP 访问路径

把后端服务暴露为类似这样的地址：

```text
https://你的-cloudbase-域名/api/health
```

能打开并返回 JSON，就说明后端接口可访问。

### 5. 配置前端接口地址

打开 `cloud-config.js`，把空字符串改成你的云端域名。

示例：

```js
window.LZDDD_API_BASE = "https://你的-cloudbase-域名";
```

注意不要写到 `/api/health`，只写域名根地址。网页会自己请求 `/api/data`、`/api/sync-news` 这些接口。

### 6. 上传前端网页

上传这两个文件到 CloudBase 静态网站托管：

```text
index.html
cloud-config.js
```

上传成功后，CloudBase 会给你一个 HTTPS 网址。公司电脑以后打开这个网址就可以使用。

### 7. 第一次使用

1. 公司电脑打开正式网址。
2. 页面会先进入登录页。
3. 输入你在 `APP_USERNAME` 和 `APP_PASSWORD` 设置的账号密码。
4. 进入工作台后，新增客户、随记、事项和抓取资讯都会自动保存到 CloudBase 云数据库。
5. 另一台电脑打开同一个正式网址并输入同一个账号密码，就能看到同一份数据。

## 以后怎么用

- 公司电脑只需要打开正式网址。
- 记录客户、事项、随记会实时保存到云数据库。
- 抓取国内资讯走云端后端，不依赖公司电脑安装环境。
- 海外参考仍然可能受网络影响，日常可以不用点。

## 安全提醒

- 不要把 `APP_PASSWORD` 设成生日、姓名、手机号。
- 不要把 DeepSeek API Key 写进 `index.html` 或 `cloud-config.js`。
- AI Key 只放在云函数环境变量 `AI_API_KEY`。
- 前端网址可以收藏，但账号密码不要随便发给别人。

## 当前代码已经支持的云端接口

```text
GET  /api/data
POST /api/data
POST /api/sync-news
GET  /api/reminders
GET  /api/ai-config
POST /api/ai-config
POST /api/ai
POST /api/cloud-push
GET  /api/health
```

## 未来可以继续升级

- 更完整的多账号权限
- 自动每天早上抓取国内资讯
- 多设备编辑冲突处理
- 数据按客户、事项、随记拆成多张表
- 手机端适配
- 主管汇报一键导出

## 官方文档入口

- CloudBase 总览：https://docs.cloudbase.net/
- 静态网站托管：https://docs.cloudbase.net/hosting/introduce
- 云函数：https://docs.cloudbase.net/cloud-function/introduce
- 文档型数据库：https://docs.cloudbase.net/database/introduce
- HTTP 访问服务：https://docs.cloudbase.net/service/introduce
